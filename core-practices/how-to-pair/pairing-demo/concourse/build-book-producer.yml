---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: pairing-demo

#outputs:
#- name: pairing-demo

run:
  path: bash
  args:
    - "-c"
    - "echo hello; ls -ltr"
#    - "ls -ltr; pwd; cd ./core-practices/how-to-pair/pairing-demo/; ./gradlew book-producer:assemble"
