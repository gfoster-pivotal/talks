---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: pairing-demo

outputs:
- name: pairing-demo

run:
  path: bash
  args:
    - "-c"
    - "cd ./core-practices/how-to-pair/pairing-demo/; ./gradlew consumer:assemble"
